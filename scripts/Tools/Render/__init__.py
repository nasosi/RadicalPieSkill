"""The render pipeline: `.pie` text to the pictures Radical Pie itself draws.

`Svg.RenderSvg` is the whole of it. Import it as `from Tools.Render.Svg import RenderSvg`;
`python -m Tools.Render svg <input.pie> <output.svg>` is the command line over the same function.
"""
